{
  "name": "task-tracker-mern",
  "version": "1.0.0",
  "private": true,
  "description": "MERN stack task tracker with CRUD, validation, MongoDB integration, and responsive React UI.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client"
  },
  "keywords": ["mern", "react", "express", "mongodb", "task-tracker"],
  "license": "MIT",
  "devDependencies": { "concurrently": "^8.2.2" }
}
